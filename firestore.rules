rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Tracked flights: readable by anyone, writable only by server (admin SDK)
    match /tracked-flights/{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
